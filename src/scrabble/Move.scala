package scrabble

// Think about how to deal with blank letters. Seperate method?

class Move(
    game: Game,
    placedLetters: List[(Pos, Letter)],
    blanks: List[Char]) {
  
  /** Returns the a new game state if the move is valid, otherwise returns appropriate error */
//  def makeMove : Either[java.lang.Throwable, Game ]
		
}

